# Dev defaults. `npm run dev` picks these up automatically.
# With mocks on, VITE_API_BASE_URL is irrelevant (the service worker intercepts
# /api/v1/* in-browser). Set VITE_ENABLE_MOCKS=false and point the URL at a real
# backend once one exists.
VITE_API_BASE_URL=/api/v1
VITE_ENABLE_MOCKS=true
