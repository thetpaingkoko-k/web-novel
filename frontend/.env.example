# Base URL for the backend REST API. Ignored in dev while VITE_ENABLE_MOCKS=true.
VITE_API_BASE_URL=http://localhost:8080/api/v1

# Set to "true" to run the in-browser mock backend (dev only). Lets the SPA work
# with no real backend. Defaults to "true" in .env.development.
VITE_ENABLE_MOCKS=false
