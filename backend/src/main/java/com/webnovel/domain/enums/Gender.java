package com.webnovel.domain.enums;

/** Reader-supplied gender captured at registration (§4.1.1). Serialized lowercase. */
public enum Gender {
    male,
    female,
    other,
    prefer_not_to_say
}
